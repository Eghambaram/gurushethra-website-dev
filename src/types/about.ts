export interface AboutHero {
  eyebrow: string;
  headline: string;
  headlineHighlight: string | null;
  body: string;
  ctaLabel: string;
  ctaHref: string;
  image: string;
  badgeLabel: string;
  badgeValue: string;
}
